services:
  api-gateway:
    build: .
    container_name: healthsys-api-gateway
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: docker
    networks:
      - healthsys-network
    restart: unless-stopped

networks:
  healthsys-network:
    external: true